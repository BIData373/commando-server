generator client {
  provider = "prisma-client-js"
  output   = "../../src/types/prisma/"
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "../../ERD.md"
}

datasource db {
  provider = "postgresql"
}
