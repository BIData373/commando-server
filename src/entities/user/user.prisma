model User {
  id   Int    @id @default(autoincrement())
  upn  String @unique
  info Json?

  assignees   Assignee[]    @relation("assignee_users")
  permissions Permission[]
  history     TaskHistory[]
  messages    Message[]

  @@map("users")
}
