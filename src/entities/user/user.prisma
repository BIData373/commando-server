model User {
  id  Int    @id @default(autoincrement())
  upn String @unique

  /// [UserInfo]
  info Json?

  assignees   Assignee[]    @relation("assignee_users")
  permissions Permission[]
  history     TaskHistory[]
  messages    Message[]
  userViews   UserView[]

  @@map("users")
}
