model User {
  id   Int    @id @default(autoincrement())
  upn  String @unique
  
  /// [UserInfo]
  info Json?

  userTaskArchives UserTaskArchives[]
  assignees   Assignee[]    @relation("assignee_users")
  permissions Permission[]
  history     TaskHistory[]
  messages    Message[]

  @@map("users")
}
