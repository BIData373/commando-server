model User {
  id  Int    @id @default(autoincrement())
  upn String @unique

  /// [UserInfo]
  info Json?

  userViewedTasks UserViewedTasks[]
  archivedUserAssigneeTask ArchivedUserAssigneeTask[]
  assignees   Assignee[]    @relation("assignee_users")
  permissions Permission[]
  history     TaskHistory[]
  messages    Message[]
  userViews   UserView[]

  @@map("users")
}
