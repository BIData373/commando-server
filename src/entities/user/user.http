# @import ../../../global.http

### Create User — valid payload
POST {{baseUrl}}/user
...headers

{
  "upn": "http-test@army.mil"
}

{{@response
  exports.userId = response.parsedBody.id;
}}

?? status == 201

### Create User — missing upn (expect 400)
POST {{baseUrl}}/user
...headers

{}

?? status == 400

### Get all Users
GET {{baseUrl}}/user
...headers

?? status == 200

### Search Users
GET {{baseUrl}}/user/search?search=http-test
...headers

?? status == 200

### Get User by id
GET {{baseUrl}}/user/{{userId}}
...headers

?? status == 200

### Update User info
PATCH {{baseUrl}}/user/{{userId}}
...headers

{
  "info": { "rank": "General", "unit": "HQ" }
}

?? status == 200

### Delete User — non-existent id (expect 400)
DELETE {{baseUrl}}/user/99999
...headers

?? status == 400

### Cleanup — Delete User
DELETE {{baseUrl}}/user/{{userId}}
...headers

?? status == 200
